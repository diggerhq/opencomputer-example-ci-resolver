# Issue filer

An OpenComputer Serverless Agent that files a GitHub issue from a CI failure
report, through a connection whose reach is compiled into the deployment.

Walkthrough follows the first verified run. Fixtures: `fixture/`.
